// Recipe interactions
document.addEventListener('DOMContentLoaded', function () {
    // We can implement infinite scroll or advanced filtering here later
    // For now, the PHP search handles the main logic

    console.log("Recipes JS loaded");
});
